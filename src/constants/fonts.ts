// Arimo — a free, open-licensed font that is metrically identical to Helvetica.
// Bundled via @expo-google-fonts/arimo and loaded in App.tsx, so it renders the
// same on iOS and Android. With custom fonts, `fontWeight` does NOT switch
// families on Android — reference the specific weight family directly instead.
export const Fonts = {
  regular: 'Arimo_400Regular',
  bold: 'Arimo_700Bold',
} as const
